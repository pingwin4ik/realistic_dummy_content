This directory contains classes which are autoloaded.

 * Each filename but be ClassName.php
 * Files herein can be used as \ClassName
 * ClassName's file must declare its namespace as namespace Drupal\realistic_dummy_content_api;
